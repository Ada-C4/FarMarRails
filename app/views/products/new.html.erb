<%= render partial:'product_form', locals:{form_title:'Add a Product', url: vendor_products_path(vendor_id: params[:vendor_id]), method: :post} %>

<%= link_to 'Return to Vendor', @back, method: :get, class: "back-button" %>
