<div class="container">
  <h1>ALL OF THEM MARKETS</h1>
  <ul>
    <% @markets.each do |market| %>
      <li>
        <%= market.name %>
        <%= link_to "Show Market", "/markets/#{market.id}" %>
      </li>
    <% end %>
  </ul>
</div>
