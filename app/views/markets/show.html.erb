<h1><%= @market.name %></h1>

<h2>Who's sellin' stuff here?</h2>

<table>
  <% @market.vendors.each do |vendor| %>
    <tr>
      <td><%= link_to vendor.name, "/markets/#{vendor.market_id}/vendors/#{vendor.id}" %></td>
    </tr>
  <% end %>
</table>
