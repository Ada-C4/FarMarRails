<div class="container">
  <%= form_for @sale, url: {action: "create"} do |f| %>
    <fieldset>
      <legend>Add a sale:</legend>
      <%= f.collection_select(:product_id, @vendor.products, :id, :name,
prompt: "Which product was sold?") %>

      <!-- <%=f.label :amount %>
      <%=f.text_field :amount %>
      <br><br>
      <%=f.label :purchase_time %>
      <%=f.text_field :purchase_time %>
      <br><br> -->
    </fieldset>
    <br>
    <%= f.submit("Add Sale!") %>
  <% end %>
</div>
