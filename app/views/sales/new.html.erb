<div class="center">
  <%= form_for @sale, url: { action: @action } do |f| %>
    <%= f.label :amount %>
    <%= f.text_field :amount, required: true%>
    <br>
    <%= f.submit %>
  <% end %>
</div>
