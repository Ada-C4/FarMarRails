<%= form_for [@market, @vendor] do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :num_employees, 'Number of Employees' %>
    <%= f.text_field :num_employees %>

    <%= f.submit 'new vendor', :id => 'submit_button' %>
<% end %>
