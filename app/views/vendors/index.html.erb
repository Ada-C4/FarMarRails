<h1>You are acting as a vendor</h1>

<br>
<table>
  <% @vendors.each do |v| %>
    <tr>
      <td>
        <%= link_to v.name, vendor_path(v.id)  %>
      </td>
      <td>
        <%= button_to "Edit", "/vendors/#{v.id}/edit", method: :get %>
      </td>
    </tr>
  <% end %>
</table>
