<div>
  <h1>ALL THEM VENDORS</h1>
  <ul>
    <% @vendors.each do |vendor| %>
      <li>
        <%= vendor.name %>
        <%= link_to "Show Vendor", "/vendors/#{vendor.id}" %>
      </li>
    <% end %>
  </ul>
</div>
